# requirements-ci.txt — Lightweight deps for CI/CD (no GPU, CPU-only PyTorch)
# ─────────────────────────────────────────────────────────────────────────────

--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0

# Data science & numerical
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.1.0
scikit-learn>=1.4.0

# Visualization & Monitoring
matplotlib>=3.8.0
seaborn>=0.13.0
tensorboard>=2.15.0

# NLP (CPU tokenizers only — no torch dependency)
huggingface-hub>=0.19.0
tokenizers>=0.14.0
sentencepiece>=0.1.99

# Transformers CPU-only (must pin to avoid pulling torch)
transformers>=4.38.0,<5.0.0

# Explainability (shap without torch extras)
shap>=0.44.0

# Lime
lime>=0.2.0.1

# Async support
aiohttp>=3.9.0

# Data collection APIs
praw>=7.7.0
newsapi-python>=0.2.0
pytrends>=4.9.0
yfinance>=0.2.30

# Database & caching
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
asyncpg>=0.29.0
redis>=5.0.0
aiosqlite>=0.19.0

# Configuration & logging
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# API & web
fastapi>=0.109.0
uvicorn>=0.27.0
celery>=5.3.0
requests>=2.31.0
pyjwt>=2.13.0
websockets>=12.0

# Monitoring
prometheus-fastapi-instrumentator>=6.0.0
prometheus-client>=0.20.0

# Utilities
tqdm>=4.67.0
python-dateutil>=2.8.0
pytz>=2024.1

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
flake8>=7.0.0
mypy>=1.8.0
